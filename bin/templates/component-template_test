import { {{component_name}} } from "./{{component_name}}";

describe("{{component_name}}", () => {
  it("should render the text Hi, hacker!", () => {
    const wrapper = shallow(<{{component_name}} />);
    expect(wrapper.text()).toEqual("Hi, hacker!");
  });
});
